; ============================================================
; php.ini — runtime-настройки PHP внутри контейнера client
; ============================================================
; Копируется в образ на stage builder (см. корневой Dockerfile).
; ============================================================

upload_max_filesize = 50M
post_max_size = 51M
